@model KeyifyWebClient.Core.Models.FretboardWebModel

<head>
    <script lang="en" type="text/javascript">

        function SelectNote(note) {

            $.ajax({
                url: '@Url.Action("UpdateFretboardModel")',
                datatype: 'json',
                async: 'false',
                contentType: "application/json",
                type: "POST",
                data: GenerateControllerMethodParams(note, true),
                success: function (data) {
                    $("#Fretboard").html(data);
                }
            });
        }

        function DeselectNote(note) {

            $.ajax({
                url: '@Url.Action("UpdateFretboardModel")',
                datatype: 'json',
                async: 'false',
                contentType: "application/json",
                type: "POST",
                data: GenerateControllerMethodParams(note, false),
                success: function (data) {
                    $("#Fretboard").html(data);
                }
            });
        }

        function GenerateControllerMethodParams(note, remove) {
            var noteArray = new Array();

            @foreach (var note in Model.Notes.Where(n => n.Value))
            {
                @:noteArray.push("@note.Key");
            }

            if (remove === true) {
                noteArray.push(note);
            } else {
                noteArray.pop(note);
            }

            return JSON.stringify(noteArray);
        }
    </script>
</head>

<body>
<h1>Fretboard</h1>

<table class="fretboard">
    <tr>
        @for (int count = 0; count < Model.Fretcount; count++)
        {
            if (count == 0)
            {
                <th class="fretboardHeading"></th>
                continue;
            }

            <th class="fretboardHeading">
                @count
            </th>
        }
    </tr>

    @foreach (var instString in Model.Tuning)
    {
        <tr>
            @foreach (var note in instString)
            {
                if (Model.Notes.Single(a => a.Key == note).Equals(new KeyValuePair<string, bool>(note, true)))
                {
                    <td class="selectedNote" onclick="DeselectNote('@note')"><b>@note</b></td>
                    continue;
                }

                <td class="defaultNote" onclick="SelectNote('@note');">@note</td>
            }
        </tr>
    }
</table>
</body>